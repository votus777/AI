

< To do list >

1. 리턴받은 값을 DB에 넣기 
    MariaDB 연결   -> 해결


이미지 업로드 -> Base64 인코딩 -> DB 저장 -> Base64 디코딩 -> np.asarray 변환 -> cv2 형식 변환 

 cv2.imdecode( ) vs io.BytesIO()  -> cv2 승리  

이제 업로드 받은 이미지 shape 통일 (resize) 필요  -> clear cv2.resize

문제는 DB에 들어갔다 나온 이미지를 보면 손상이 너무 심하다
...엥? imwrite로 저장해서 보면 RGB 순서만 바뀌었을 뿐 잘나온다



2. DB와 file server 구성하기 

3. Web 서버를 구성해 외부에서 접속 가능하게 하기 ( 보안 조심 )

 
